// Esto es lo que hace autopublish en el lado del cliente, paquete ya eliminado
// Meteor.subscribe('quotes'); //Se elimina el suscribe porque desde iron router (/lib/router.js) se declara la suscripcion 

